# 2026-06-15

## Atomic-CRM UI改造 + 数据连接修复

### 用户反馈
- 数据不见了（前端指向已失效的公网隧道 `upset-ideas-grab.loca.lt`）
- 布局没改变（scoped样式被全局CSS变量覆盖）

### 根因
1. **数据丢失**：`request.js` 的 `baseURL` 之前为公网tunnel，tunnel断了之后所有API请求超时
2. **布局不生效**：`--crm-sidebar-bg: #1E293B`（深色）在 `design-tokens.css` 里，全局CSS变量优先级高于 LayoutV2.vue 的 scoped 样式
3. **Dashboard样式未应用**：scoped样式里只有 `.shad-list` 系列，缺少 `.stat-card-item`/`.content-card` 定义

### 修复
- `design-tokens.css`: `--crm-sidebar-bg: #FFFFFF` 等改为light配色
- `LayoutV2.vue`: 完整重写scoped样式为shadcn light sidebar
- `DashboardV2.vue`: 补全 `.stat-card-item/.content-card/.stat-grid` 等scoped样式
- `request.js` (两个): baseURL改回 `http://localhost:8081`
- `ProductList.vue`: uploadUrl改回localhost
- 重新构建+部署CloudStudio+推送GitHub

### 部署链路
- 静态站点: CloudStudio (https://6345e9a003de49c686892c95fb075565.app.codebuddy.work)
- 后端: 本地8081 (Node.js SQLite)
- 公网访问: localtunnel不稳定，已回退到本地访问模式
- 永久方案: Render.com部署待完成（已推送crm-backend到GitHub）

### 测试账号
- admin / admin123
- 13800000000 / 123456

## 22:00 第3次改造：dashboard v3 - 全中文 + 全新布局

### 用户反馈
"整体布局还是没有变，只变了颜色，且不要用英文"

### 根因
之前"改造"只改色（scoped与全局变量冲突），结构仍沿用：渐变卡+2图+3栏。真正atomic-crm是：纯白卡+1px边框+蓝色激活+紧凑字体。

### 重写
DashboardV2.vue 完全重写为：
1. 欢迎栏（蓝渐变+3个快捷按钮）
2. 4张KPI卡（带迷你柱状图sparkline）
3. 销售趋势(2/3宽) + 真实funnel漏斗(1/3宽)
4. 客户来源（环形图+legend） + 最新动态（头像+时间+金额）
5. 今日优先级 + 热销商品TOP5 + 团队排名 三栏

### 文字
全部中文（按钮/标签/提示/状态文案）。

### 部署
- vite build成功
- dev server (HMR自动) — localhost:3002
- 已推送GitHub
