# 2026-05-31 工作记录

## CRM系统前端空白页问题修复

### 问题诊断
- 预览工具打开 `http://localhost:5173` 后页面完全空白
- Vite HMR WebSocket 与 IDE 预览工具不兼容

### 解决方案
1. **禁用 Vite HMR**：在 `vite.config.js` 中添加 `hmr: false`
2. **改为生产模式构建**：使用 `npx vite build` + `npx vite preview` 替代 `npx vite dev`
3. **修复缺失的 API 文件**：
   - 创建 `src/api/product.js`（含 getProducts, getPriceStrategies, savePriceStrategies 等）
   - 创建 `src/api/quotation.js`（含 getQuotations, rejectQuotation, convertToOrder 等）
   - 创建 `src/api/opportunity.js`（含 getOpportunities 等）
   - 更新 `src/api/customer.js` 添加别名导出（getCustomers, getCustomer 等）
   - 更新 `src/api/index.js` 添加 customerApi, userApi 和 default export
4. **更新 start-crm.bat**：使用 `vite preview` 代替 `vite dev`，添加端口清理逻辑

### 当前系统状态
- 后端：Spring Boot 3.2.5 运行在 `http://localhost:8080`，H2 内存数据库
- 前端：Vite preview 运行在 `http://localhost:5177`
- 登录：admin / admin123

---

## 第二轮修复：数据库表缺失导致列表和AI失败

### 问题根因
H2 初始化脚本只创建了 13 张原始表，新模块 42+ 张表完全缺失，所有列表查询报 "Error querying database"。

### 修复内容
1. **新增全部新模块表 DDL**（线索、商机、产品、报价、日程、公告、审批等）
2. **修复 DDL 与实体类不匹配**：
   - `sys_dict_data`：label/value → dict_label/dict_value
   - `crm_product`：list_price → price，增加 model/specification 字段
   - `sys_config`：增加 config_name/config_type
   - `crm_approval_flow`：增加 updated_at
   - `crm_service_request`：增加 request_no/contact_id/owner_id 等
3. **H2 保留字修复**：`application.yml` 添加 `NON_KEYWORDS=MONTH`
4. **前端 API 路径修复**：analytics/dashboard → dashboard/stats
5. **加载种子数据**：产品8条、客户8条、线索5条、商机5条、活动3条、会员4条
6. **JAR 更新方法**：Python zipfile 方案绕过 Maven 重建（环境受限无完整 Maven）

### 当前状态
- 后端：`http://localhost:8080`（所有关键 API 200）
- 前端：`http://localhost:3001`（vite preview 生产模式）
- 登录：admin / admin123

---

## 生成 CRM 开发规格文档

基于当前系统完整功能梳理，生成了 `CRM系统开发规格文档.md`，包含：
- 27个模块、30个Controller、53张表、36个Vue页面的完整架构
- 180+ API端点的完整规范
- 数据库53张表的字段定义
- 5阶段实施路线图（MVP → 智能化）
- AI智能化能力规格（9个端点 + 助手指令集）

---

## 全新 Node.js + SQLite 后端系统搭建

基于规格文档直接开发出可运行系统：
- **新后端**：Express.js + better-sqlite3，`crm-server/` 目录，端口 8081
- **数据库**：SQLite 本地文件 `data/crm.db`，40+ 张表完整建表 + 种子数据
- **API覆盖**：认证、客户、线索、商机、产品、订单、营销、会员、AI、分析等全模块
- **前端改造**：API baseURL 指向 8081，Vue3 前端无需修改
- **启动方式**：`node src/server.js` 即可，无 Maven/Java 依赖

### 当前运行
- 后端：`http://localhost:8081` (Node.js Express)
- 前端：`http://localhost:3002` (Vite preview)
- 登录：admin / admin123

---

## 修复产品同步 + CRM门户管理功能

### 问题
1. 会员门户产品列表报 `sales_count` 列不存在 → crm_product DDL 缺列
2. CRM后台无门户管理功能，无法调整客户端Banner/公告/产品上下架

### 修复
1. **DDL**：crm_product 加入 `member_price`, `sales_count`, `images`, `is_on_sale`
2. **门户管理API**：`/api/admin/portal-configs` CRUD、portal-stats、产品toggle-sale/member-price
3. **门户管理页面**：`views/system/PortalManage.vue`（概览+Banner编辑+产品表格+用户管理）
4. **产品过滤**：会员门户产品列表加 `WHERE is_on_sale=1`
5. **完整商品**：28款名创优品产品全部加载并上架

### 访问
- CRM → 系统设置 → 🛒 门户管理
- 会员门户 → 首页可见28款产品
- 登录：admin / admin123
